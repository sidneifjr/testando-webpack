version: '3'

services:
  node:
    build:
      context: .
      dockerfile: docker/node/Dockerfile
    ports:
      - '9000:9000'
      - '8080:8080'
    volumes:
      - .:/frontend
    command: npm run build
    environment:
      - CHOKIDAR_USEPOLLING=1